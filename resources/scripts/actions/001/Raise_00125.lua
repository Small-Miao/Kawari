-- 复活 / Raise — revival (and Weakness application) is handled server-side (servers/world/src/server/jobs/whm.rs).

function doAction(player, in_combo)
    effects = EffectsBuilder()

    return effects
end
