-- 以太步 / Aetherial Manipulation
-- Movement is performed client-side; all mechanics are handled server-side (servers/world/src/server/jobs/blm.rs).

function doAction(player, in_combo)
    effects = EffectsBuilder()

    return effects
end
