-- 庇护所 / Asylum — the ground area, its ticks, and the EFFECT_ASYLUM_INSIDE status are
-- handled server-side (servers/world/src/server/jobs/whm.rs).

function doAction(player, in_combo)
    effects = EffectsBuilder()

    return effects
end
