-- 礼仪之铃 / Liturgy of the Bell — bell placement, stacks, and triggers are handled
-- server-side (servers/world/src/server/jobs/whm.rs).

function doAction(player, in_combo)
    effects = EffectsBuilder()

    return effects
end
